<Window x:Class="user_client.View.BoardListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:user_client.View"
        mc:Ignorable="d"
        Title="BoardListWindow" Height="450" Width="800">
    <Grid>
        <Button Content="👤" Margin="730,27,20,357" Height="30" Width="30" FontSize="18"/>
        <TextBlock Text="전체 글 보기" FontSize="20" FontWeight="Bold"
                   Margin="50,80,0,0" />
        <TextBlock Text="23,349개의 글" FontSize="12"
                   Margin="50,120,0,0" />
        <DataGrid Name="PostGrid" Margin="50,144,30,24" AutoGenerateColumns="False"
          Background="White"
          HeadersVisibility="Column" BorderThickness="0" GridLinesVisibility="None">

            <!-- 열 정의 -->
            <DataGrid.Columns>
                <!-- 상태 -->
                <DataGridTemplateColumn Header="상태" Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Status}" TextAlignment="Center" Background="White" Padding="5" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- 제목 -->
                <DataGridTextColumn Header="제목" Binding="{Binding Title}" Width="*"/>

                <!-- 작성자 -->
                <DataGridTextColumn Header="작성자" Binding="{Binding Author}" Width="100">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextAlignment" Value="Center" />
                            <Setter Property="VerticalAlignment" Value="Center" />
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <!-- 작성일 -->
                <DataGridTextColumn Header="작성일" Binding="{Binding Date}" Width="150">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextAlignment" Value="Center" />
                            <Setter Property="VerticalAlignment" Value="Center" />
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>

            <!-- 행 스타일 -->
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                    <!-- 아랫부분 테두리만 -->
                    <Setter Property="BorderBrush" Value="LightGray"/>
                    <!-- 테두리 색상 -->
                    <Setter Property="Padding" Value="2"/>
                </Style>
            </DataGrid.RowStyle>

            <!-- 셀 스타일 -->
            <DataGrid.CellStyle>
                <Style TargetType="DataGridCell">
                    <Setter Property="BorderThickness" Value="0"/>
                    <!-- 셀 테두리 제거 -->
                </Style>
            </DataGrid.CellStyle>
        </DataGrid>
        <ComboBox HorizontalAlignment="Right" VerticalAlignment="Top" Width="70"
                  Margin="0,120,30,0" SelectedIndex="0">
            <ComboBoxItem Content="15개씩" />
            <ComboBoxItem Content="30개씩" />
        </ComboBox>
    </Grid>
</Window>
