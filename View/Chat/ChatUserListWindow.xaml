<Window x:Class="user_client.View.Chat.ChatUserListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:user_client.View.Chat"
        mc:Ignorable="d"
        Background="#e1e1e1"
        xmlns:vm="clr-namespace:user_client.ViewModel"
        Title="ChatUserListWindow" Height="670" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="10*" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <TextBox Width="300" Margin="15 10 0 10" Background="Transparent"></TextBox>
            <Button Click="AddChatRoomButton_Click" BorderBrush="Transparent">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderBrush" Value="Transparent"/>
                        <Setter Property="OverridesDefaultStyle" Value="True"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
                <Image Source="/Src/Icon/CreateUserChatIcon.png" Width="40" Margin="18 0 15 0"/>
            </Button>

        </StackPanel>
        <ItemsControl ItemsSource="{Binding RecentChattingUsers}" Grid.Row="1" Width="Auto" Height="Auto" Background="Transparent">
            
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Width="400" MouseLeftButtonDown="User_MouseLeftButtonDown" BorderBrush="#B0B0B0" BorderThickness="0 0 0 1" Padding="10" Margin="-5 0 0 0">
                        <Grid >
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="50"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Image Source="/Src/Icon/Profile.png"></Image>

                            <StackPanel Grid.Column="1" Margin="10 0 0 0" VerticalAlignment="Center" HorizontalAlignment="Left">
                                <TextBlock Text="{Binding Name}"
                                           FontSize="16"
                                           Width="250"
                                           FontWeight="SemiBold"
                                           Foreground="#333333"
                                               TextTrimming="CharacterEllipsis"
                                           TextWrapping="NoWrap"
                                           />
                                <TextBlock Text="{Binding RecentChattingLog}"
                                           FontSize="12"
                                           Foreground="#666666"
                                           Margin="0,2,0,0"
                                           Width="250"
                                           TextWrapping="NoWrap"
                                               TextTrimming="CharacterEllipsis"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</Window>
