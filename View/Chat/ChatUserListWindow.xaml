<Window x:Class="user_client.View.Chat.ChatUserListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:user_client.View.Chat"
        mc:Ignorable="d"
        Background="#e1e1e1"
        xmlns:vm="clr-namespace:user_client.ViewModel"
        Title="ChatUserListWindow" Height="670" Width="400">
    <Window.DataContext>
        <vm:ChatUserListViewModel />
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="10*" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <TextBox Width="320" Margin="15 10 0 10" Background="Transparent"></TextBox>
            <Image Source="/Src/Icon/CreateUserChatIcon.png" Width="40" Margin="15 0 15 0"></Image>
        </StackPanel>
        <ListBox Grid.Row="1" Width="Auto" Height="Auto" Background="Transparent">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Width="Auto" Height="100" BorderBrush="#B0B0B0" BorderThickness="0 0 0 1" Padding="10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="50"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Image Source="/Src/Icon/profile.png"></Image>

                            <StackPanel Grid.Column="1" Margin="10 0 0 0" VerticalAlignment="Center">
                                <TextBlock Text="{Binding Name}"
                                           FontSize="16"
                                           FontWeight="SemiBold"
                                           Foreground="#333333"/>
                                <TextBlock Text="{Binding RecentChattingLog}"
                                           FontSize="12"
                                           Foreground="#666666"
                                           Margin="0,2,0,0"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
