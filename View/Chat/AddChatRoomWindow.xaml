<Window x:Class="user_client.View.Chat.AddChatRoomWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:user_client.View.Chat"
        mc:Ignorable="d"
        Background="#e1e1e1"
        Title="AddChatRoomWindow" Height="420" Width="280">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="*" />
            <RowDefinition Height="40" />
        </Grid.RowDefinitions>

        <ScrollViewer  Grid.Row="0" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
            <ItemsControl ItemsSource="{Binding SelectedUsers}" Padding="10 0">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Width="80" Height="25" BorderBrush="#8c8c8c" BorderThickness="1" Margin="0 0 15 0">
                            <DockPanel>
                                <TextBlock FontSize="10" Width="70" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" Padding="5 0" VerticalAlignment="Center" HorizontalAlignment="Center" Text="{Binding Name}">양은석[STAFF]</TextBlock>
                                <Border Width="10" Height="10" BorderBrush="#8c8c8c" Margin="0 -20 -8 0" Background="#e1e1e1"  BorderThickness="1" CornerRadius="100">
                                    <Button Width="8" Height="8" VerticalAlignment="Center" HorizontalAlignment="Center" Style="{x:Null}" Foreground="black" BorderThickness="0" Background="Transparent" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" FontSize="8">
                                        <TextBlock Margin="0 -3 0 0">x</TextBlock>
                                    </Button>
                                </Border>
                            </DockPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>



            <Grid Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <ScrollViewer Grid.Row="0">
                    <StackPanel>
                    <TextBlock Margin="10 1" FontSize="14">관리자</TextBlock>
                        <ItemsControl ItemsSource="{Binding AdminList}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Grid.Row="1" Width="250" Height="40" BorderBrush="#8c8c8c" BorderThickness="0 1 0 0">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="60" />
                                            </Grid.ColumnDefinitions>

                                            <TextBlock Grid.Column="0" Text="{Binding Name}" VerticalAlignment="Center" Margin="20 0 0 0"></TextBlock>
                                            <Button Click="InviteUserButton_Click" Grid.Column="1" Width="40" Height="25" FontSize="22" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" Padding="0 -6 0 0" Margin="0 0 15 0">+</Button>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </ScrollViewer>
                <ScrollViewer Grid.Row="1">
                <StackPanel>
                    <TextBlock FontSize="14" Margin="10 1">사원</TextBlock>
                    <ItemsControl ItemsSource="{Binding StaffList}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Grid.Row="1" Width="250" Height="40" BorderBrush="#8c8c8c" BorderThickness="0 1 0 0">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="60" />
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Grid.Column="0" Text="{Binding Name}" VerticalAlignment="Center" Margin="20 0 0 0"></TextBlock>
                                        <Button Click="InviteUserButton_Click" Grid.Column="1" Width="40" Height="25" FontSize="22" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" Padding="0 -6 0 0" Margin="0 0 15 0">+</Button>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
                </ScrollViewer>
        </Grid>

        <Button Click="AddChatRoomButton_Click"  Grid.Row="2" BorderBrush="#8c8c8c" BorderThickness="1" Width="100" Height="30" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0 0 10 0">채팅방 만들기</Button>
    </Grid>
</Window>
